import {makeStyles} from "@material-ui/styles";

const useStyles = makeStyles({

    header: {
        textAlign: "center",
        fontWeight: "bolder"
    },

    paragraf: {
        textAlign: "center"
    },

    materialIconDige: {
        width: "100%",
        height: "auto"
    }
});


export default useStyles;