import {makeStyles} from "@material-ui/styles";

const useStyles = makeStyles({

    root: {
        paddingTop: "50px"
    },

    header: {
        textAlign: "center",
        fontSize: "80px",
        fontWeight: "bolder"
    },

    headerTwo : {
        textAlign: "right"
    },

    explaination: {
        textAlign: "right"
    }
});


export default useStyles;